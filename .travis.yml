sudo: false
language: php
branches:
  only:
  - master
notifications:
  email: false

jobs:
  include:
  - <<: *phpunit
    php: 5.6
    install:
    - composer update --no-dev
    - php -d phar.readonly=0 script/build_phar.php
    - sh script/upload_phar.sh
